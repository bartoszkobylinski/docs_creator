[mutmut]
# Paths to mutate
paths_to_mutate=fastdoc/,services/,api/,core/

# Paths to exclude from mutation
paths_to_exclude=tests/,__pycache__,*.pyc,migrations/,venv/,virtualenv/

# Test command
runner=poetry run python -m pytest tests/ -x --tb=no --disable-warnings

# Test discovery timeout
timeout=300

# Mutants to skip (known issues or intentional mutations to ignore)
# Format: filename:line_number
skip=

# Enable/disable specific mutation types
# dict_literal=True
# argument_mutations=True
# string_literals=True
# comprehensions=True

# Additional options
use_coverage=True
coverage_data=.coverage
backup=True

# Parallel execution
# processes=auto